# الفلاشة/الـSD Card كـ "كمبيوتر محمول" — دليل البناء الكامل

**المشروع:** نظام Ubuntu مبني كـ Full Install (مش Live+Persistence) على SD Card بمساحة 128GB، يقلع من أي جهاز x86_64 (BIOS أو UEFI)، خفيف جداً (Openbox فقط)، بدون برامج مسبقة، مع قسم داتا مشفّر (LUKS) للشغل.

**القرارات المتفق عليها:**

| القرار | الاختيار |
|---|---|
| أسلوب البناء | Full Install حقيقي (مش squashfs/persistence) |
| التوزيعة الأساس | Ubuntu Server 26.04 LTS (Resolute Raccoon) — minimal |
| الواجهة الرسومية | Openbox + panel بسيط (tint2) |
| طريقة الوصل | قارئ USB خارجي ثابت (مو قارئ SD داخلي باللابتوب) |
| تشفير الداتا | نعم — LUKS على قسم منفصل |

---

## 1. لمحة عامة على البنية (Architecture)

```
SD Card (GPT, 128GB) عبر قارئ USB
├─ Partition 1: bios_grub        (1MB)      — يسمح بالإقلاع من BIOS/Legacy على قرص GPT
├─ Partition 2: EFI System (ESP) (512MB, FAT32) — يسمح بالإقلاع من UEFI
├─ Partition 3: / (root)         (20GB, ext4, غير مشفّر) — نظام التشغيل نفسه
└─ Partition 4: DATA             (~107GB, LUKS → ext4 بالداخل) — داتا شغلك، مشفّرة
```

**ليش هيك التقسيم؟**
- نظام التشغيل نفسه (root) ما في داعي يكون مشفّر — هيك بتقلع بسرعة بدون ما تدخل كلمة سر بكل مرة، وأي عطل بالنظام بتقدر تصلحه بسهولة (chroot من live USB مثلاً).
- الداتا الحساسة (شغلك) هي يلي بدها حماية فعلياً — لأنك رح تحطها بأجهزة غريبة. فبتنطلب كلمة سر وقت الإقلاع أو وقت التوصيل عشان تفك تشفيرها.
- الفصل بين OS وDATA كمان بسهّل عليك مستقبلاً: لو حبيت تعيد تنصيب النظام أو تغيّر التوزيعة، الداتا محفوظة بقسمها الخاص وما بتنلمس.

**نقطة حرجة لازم تنتبهلها: BIOS + UEFI مع بعض**
عملية تنصيب Ubuntu (subiquity installer) بتظبط الـ bootloader حسب الوضع يلي أقلعت فيه الـ installer نفسه (يعني إذا أقلعت الـ installer بـ UEFI، رح يركب لك grub-efi بس، وما رح يشتغل على أجهزة قديمة BIOS-only). لهيك، بعد التنصيب، **لازم خطوة يدوية إضافية** (موجودة بالقسم 4 تحت) لتركيب grub-pc كمان عشان الفلاشة تشتغل بالحالتين.

---

## 2. الأدوات يلي بدك ياها قبل ما تبلش

- جهاز فيه Linux (أو VM عليه Linux — VirtualBox/QEMU) لتحضير الفلاشة، أو تقدر تشغّل الـ installer مباشرة من نفس الفلاشة.
- ISO تبع **Ubuntu Server 26.04 LTS** (النسخة minimal/Server — مو Desktop — لأنها الأخف وما فيها واجهة رسومية أصلاً، إحنا رح نضيف Openbox يدوياً).
- أداة لحرق الـ ISO على فلاشة تنصيب مؤقتة (Rufus على ويندوز، أو `dd`/balenaEtcher على لينكس) — هاي فلاشة "التنصيب" منفصلة عن الـ SD Card الهدف.
- قارئ USB خارجي (يلي قررت تستخدمه دايماً) موصول فيه الـ SD Card 128GB (هاد هو "القرص الهدف").
- **تنبيه هام:** انتبه كتير أي قرص (`/dev/sdX`) تختاره أثناء التقسيم — أي غلطة هون ممكن تمسح قرص غلط. تأكد بالـ `lsblk` قبل أي خطوة تدمير.

---

## 3. التقسيم والتنصيب

### خيار أ) تنصيب يدوي تفاعلي (الأسهل للمرة الأولى، موصى فيه)

1. قلّع من فلاشة تنصيب Ubuntu Server عبر **UEFI** (فعّل UEFI مو Legacy بالـ boot menu، هاد بيضمن التوافق الأوسع، وبعدين منضيف BIOS يدوياً).
2. بالـ installer، اختار **Custom storage layout** (مو Guided) على القرص يلي هو الـ SD Card (تأكد من الحجم 128GB بالضبط قبل ما تكمل).
3. اعمل التقسيم يدوياً بالضبط متل الجدول فوق:
   - Partition 1: 1MB, flag = `bios_grub`, بدون filesystem.
   - Partition 2: 512MB, flag = `boot/esp`, format = FAT32, mount = `/boot/efi`.
   - Partition 3: 20GB, format = ext4, mount = `/`.
   - Partition 4: الباقي (~107GB) — بالـ installer الحديث فيه خيار "Use as encrypted volume (LUKS)" لما تحدد نوع القسم. حدده، حط كلمة سر قوية (احفظها كويس — لو نسيتها الداتا منها رجعة)، وبعدين format = ext4, mount = مثلاً `/mnt/data`.
4. كمّل التنصيب العادي (hostname, username, timezone... الخ). **لا تنصّب أي snaps إضافية** وقت التنصيب (بس الأساسيات) — هيك النظام يضل خفيف.

### خيار ب) Autoinstall آلي (لو بدك تكرر العملية أو تؤتمتها)

مرفق ملف `autoinstall-user-data.yaml` كنموذج بدايةً — **لازم تعدله وتجربه على VM قبل ما تستخدمه على الـ SD Card الحقيقية**، لأن:
- الـ `path`/`match` تبع القرص لازم يطابق القرص الصح بالضبط (خطر مسح قرص غلط).
- صياغة autoinstall بتتغير شوي بين إصدارات Ubuntu — تأكد من مطابقتها لتوثيق [Subiquity الرسمي](https://canonical-subiquity.readthedocs-hosted.com/en/latest/reference/autoinstall-reference.html) لنسخة 26.04 بالضبط وقت التنفيذ.
- كلمة سر الـ LUKS موجودة نص صريح (plaintext) بالملف أثناء البناء فقط — **غيّرها فوراً بعد أول إقلاع** عبر `cryptsetup luksChangeKey` واحذف الملف من أي مكان غير آمن.

---

## 4. خطوة حرجة: تفعيل الإقلاع الهجين (BIOS + UEFI)

بعد ما تخلص التنصيب وتقلع بنجاح أول مرة (عبر UEFI)، افتح ترمينال جوا النظام المثبت ونفّذ:

```bash
sudo apt update
sudo apt install -y grub-pc-bin grub-pc

# حدد اسم القرص نفسه (مش partition) — مثلاً /dev/sda وليس /dev/sda3
lsblk

# ركّب GRUB لنسخة BIOS/Legacy كمان على نفس القرص
sudo grub-install --target=i386-pc /dev/sdX   # استبدل sdX بالقرص الصحيح

# تأكد إنو partition 1 (bios_grub) موجود ومحدد صح، غير هيك الأمر فوق بيفشل
sudo update-grub
```

هلق الفلاشة صارت تقلع من أي جهاز — قديم (BIOS) أو حديث (UEFI) — بدون ما تغيّر أي إعداد بالـ BIOS تبع الجهاز المضيف (بس تأكد إنو "USB Boot" مفعّل بالـ boot menu، وهاد إعداد قياسي بكل الأجهزة).

**ملاحظة Secure Boot:** إذا الجهاز المضيف فيه Secure Boot مفعّل، Ubuntu الرسمي عادة بيشتغل معه لأنو shim موقّع من Microsoft. لو صادفت مشكلة إقلاع بجهاز معيّن، أول حل تجربه هو تعطيل Secure Boot من BIOS/UEFI تبع هداك الجهاز تحديداً (إعداد بالجهاز المضيف نفسه، مش بالفلاشة).

---

## 5. تركيب الواجهة الرسومية (Openbox) والأدوات الأساسية

استخدم ملف `02-post-install-setup.sh` المرفق. شغّله مرة وحدة بعد أول إقلاع (لازم اتصال إنترنت — الأسهل عبر كيبل إيثرنت أول مرة):

```bash
chmod +x 02-post-install-setup.sh
sudo ./02-post-install-setup.sh
```

الملف بينصّب: Openbox + tint2 (panel) + pcmanfm (مدير ملفات) + lxterminal + rofi (لفتح البرامج، لأنو Openbox ما فيه قائمة برامج جاهزة) + lightdm (شاشة دخول) + إدارة شبكة (NetworkManager + nm-applet لليوايفاي) + zram (سواب بالذاكرة بدل الكتابة عالـ SD Card، لحماية عمر الكارت) + الإعدادات الأساسية لـ autostart.

بعدها بتقدر من التيرمينال (Ctrl+Alt+T أو من rofi) تنصّب أي برنامج بدك ياه بـ:
```bash
sudo apt install <package-name>
```

---

## 6. ضبط قسم الداتا المشفّر (يومياً)

بعد أول boot، القسم المشفّر لازم يتفكّ تلقائياً وقت الإقلاع (هيك حطيناه بالـ `/etc/crypttab`) — رح يطلب منك كلمة السر بشاشة سوداء قبل ما توصل لواجهة تسجيل الدخول. هاد طبيعي ومقصود: أي حدا لاقط الفلاشة ما بيقدر يوصل لداتاك بدون الباسورد.

لتغيير كلمة السر بأي وقت:
```bash
sudo cryptsetup luksChangeKey /dev/sdX4   # partition 4 - الداتا
```

مجلد الداتا موجود على `/mnt/data` — اربطه بمجلد شغلك (مثلاً اعمل symlink من `~/Work` إله) عشان يكون سهل الوصول.

---

## 7. نصائح للعمر الافتراضي والأداء (SD Card تحديداً)

- استخدم كارت SD من فئة **UHS-I على الأقل** (الأفضل UHS-II إذا القارئ يدعمها) — كتابة عشوائية بطيئة بتخنق النظام كامل.
- تجنب الـ swap على القرص نهائياً (استخدمنا zram بدلها بالسكربت).
- فعّل `noatime` على mount options لتقليل الكتابة الزائدة (مضاف بالسكربت لملف `/etc/fstab`).
- لو القارئ USB يدعم UASP/TRIM، فعّل `fstrim.timer` (مضاف بالسكربت أيضاً، ما بيأذي حتى لو مو مدعوم).
- كل ما قل عدد مرات الكتابة (logs, cache) كل ما زاد عمر الكارت — فكر لاحقاً بتحويل `/var/log` لـ tmpfs إذا حسّيت في كتابة كتيرة.

---

## 8. قائمة اختبار قبل ما تعتمد عليها فعلياً

- [ ] تقلع بنجاح على جهاز UEFI حديث.
- [ ] تقلع بنجاح على جهاز BIOS/Legacy قديم (أو UEFI مضبوط على CSM/Legacy mode).
- [ ] الشبكة (سلكي ولاسلكي) بتشتغل وبتاخد IP تلقائياً على أجهزة مختلفة.
- [ ] قسم الداتا بيطلب الباسورد وبيتفك صح، والملفات يلي ضفتها ببقوا بعد إعادة التشغيل.
- [ ] جرب تفصل الفلاشة من جهاز وتوصلها بجهاز تاني تماماً مختلف (شركة/موديل مختلف) وشوف الإقلاع + الشبكة + الداتا كلهم شغالين.
- [ ] تأكد إنو تنصيب برنامج جديد عبر `apt install` وحفظ ملف بالداتا بضلوا موجودين بعد إعادة تشغيل كامل.

---

## 9. أفكار توسّع مستقبلية (اختيارية، مو ضرورية هلق)

- تشفير قسم الـ root كمان (full-disk encryption) لو بدك حماية أعلى (بثمن: كلمة سر بكل إقلاع حتى قبل ما توصل للنظام).
- عمل نسخة احتياطية (backup image) للـ SD Card كامل بعد ما تضبطها منيح، عشان لو تعطلت تقدر ترجّعها بسرعة بدل ما تعيد كل الخطوات.
- إضافة سكربت "provisioning" شخصي (dotfiles + قائمة برامجك المفضلة) تشغّله مرة وحدة على أي نسخة جديدة.
